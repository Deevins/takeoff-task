import React from "react";

const Form: React.FC = () => {
  return (
    <div>
      <form>
        <input type="email" />
        <input type="password" />
        <button type={"submit"} />
      </form>
    </div>
  );
};

export default Form;
